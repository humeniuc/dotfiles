TMUX_SESSIONIZER_PATHS="$HOME
$HOME/dotfiles
$HOME/dir1
$HOME/dir2
";

TMUX_SESSIONIZER_PATHS="$TMUX_SESSIONIZER_PATHS"$'\n'$(find "$HOME/path/of/projects/dir" -maxdepth 1 -mindepth 1 -type d)

echo "$TMUX_SESSIONIZER_PATHS"

export TMUX_SESSIONIZER_PATHS
