#!/bin/sh

systemctl --user import-environment DISPLAY XAUTHORITY
# Source X session environment
[ -f "$HOME/.xprofile" ] && . "$HOME/.xprofile"

exec i3 \
    </dev/null \
    >"$HOME/.i3.log" \
    2>&1
