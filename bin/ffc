#!/bin/bash

function start_ff
{
    PROFILEDIR=$(mktemp -d)
    firefox --new-instance --profile "$PROFILEDIR"
    rm -rf "$PROFILEDIR"
}

start_ff &
# TODO: studiaza cum este cu double-fork
# (start_ff &) &
