#!/bin/bash
# afiseaza intreg istoricul unui fisier
# apeleaza cu svnchanges file | less

url=$1
svn log -q $url | grep -E -e "^r[[:digit:]]+" -o | cut -c2- | while read rev
do
        echo
        svn log -r $rev $url@HEAD
        svn diff -c $rev $url@HEAD
        echo
done

